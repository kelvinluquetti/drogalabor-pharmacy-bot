import unittest\n\nclass TestFAQSearch(unittest.TestCase):\n    def setUp(self):\n        # Setup code to initialize the FAQ search functionality (e.g., mock database)\n        self.faq_data = {\n            'What is the return policy?': 'You can return items within 30 days.',\n            'How to contact support?': 'You can contact support at support@example.com.',\n        }\n\n    def test_faq_search_existing_question(self):\n        question = 'What is the return policy?'\n        answer = self.faq_data.get(question)\n        self.assertEqual(answer, 'You can return items within 30 days.')\n\n    def test_faq_search_non_existing_question(self):\n        question = 'What is the shipping policy?'\n        answer = self.faq_data.get(question)\n        self.assertIsNone(answer)\n\n    def test_faq_search_partial_match(self):\n        question = 'return policy'\n        # This would normally involve a search method that handles partial matches\n        answer = next((ans for qst, ans in self.faq_data.items() if question.lower() in qst.lower()), None)\n        self.assertEqual(answer, 'You can return items within 30 days.')\n\nif __name__ == '__main__':\n    unittest.main()